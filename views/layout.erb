<!doctype html>
<html lang="en-US">
  <head>
    <title>Todo List App</title>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="//fonts.googleapis.com/css?family=PT+Sans">
    <link rel="stylesheet" href="/stylesheets/whitespace-reset.css">
    <link rel="stylesheet" href="/stylesheets/application.css">
    <script src='/javascripts/jquery.js'></script>
    <script src='/javascripts/application.js'></script>
  </head>
  <body>
    <header>
      <h1>Todo Tracker</h1>
      <%# We define the specific content on the page we want it to display %>
      <%# This is just a placeholder the content %>
      <%== yield_content :header_links %>
    </header>
    
    <% if session[:error] %>
      <div class="flash error">
        <p><%= session.delete(:error) %></p>
      </div>
    <% end %>

    <% if session[:success] %>
      <div class="flash success">
        <%# This method allows us to delete a key value pair, and return that deleted value %>
        <%# On subsequent page views, session[:success] will not exist, and so the box will not show up %>
        <p><%= session.delete(:success) %></p>
      </div>
    <% end %>

    <main>
      <%== yield %>
    </main>
  </body>
</html>
